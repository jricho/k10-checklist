apiVersion: v1
kind: Service
metadata:
  name: k10-checklist
  labels:
    app.kubernetes.io/name: k10-checklist
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: k10-checklist
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
